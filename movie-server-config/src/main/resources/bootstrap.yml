server:
  port: 8888
spring:
  application:
    name: movie-server-config
  profiles:
    # 启用本地存储配置
    active: native
  cloud:
    config:
      server:
        # 本地配置
        native:
          # 配置文件所在目录
#          search-locations: classpath:/config
          search-locations: /home/humingk/gits/douban_movie_plus/doc/config
#    config:
#      server:
#        # git远程配置
#        git:
#          # git远程仓库地址,访问url:  /{application}/{profile} eg: /movie-server-config/dev
#          uri: https://github.com/humingk/douban_movie_config
#          # 仓库路径
#          search-paths: config
#      # 仓库分支
#      label: master
#    # 总线
#    bus:
#      enabled: true
#      trace:
#        # 开启跟踪总线
#        enabled: true
#  # 消息中间件rabbitmq
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#management:
#  endpoint:
#    bus-refresh:
#      enabled: true
#  endpoints:
#    web:
#      exposure:
#        # 暴露接口
#        include: bus-refresh
